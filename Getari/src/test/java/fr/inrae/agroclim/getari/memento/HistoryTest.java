package fr.inrae.agroclim.getari.memento;

import org.junit.Test;

import lombok.extern.log4j.Log4j2;
@Log4j2
public class HistoryTest {

    @Test
    public void memTest() {
        //        History history = new History();
        //        assertEquals(0, history.getSize());
        //        Evaluation e = new Evaluation();
        //        e.setName("fr", "Eval1");
        //        Origin o = new Origin();
        //        o.setEvaluation(e);
        //        history.addMemento(o.save("bla"));
        //        assertEquals("size != 1", 1, history.getSize());
        //        assertEquals("name != Eval1", "Eval1", history.getMemento().getEvaluation().getName());
        //        Evaluation e2 = e.clone();
        //        e2.setName("fr", "Eval2");
        //        o.setEvaluation(e2);
        //        history.addMemento(o.save("bli"));
        //        assertEquals("size != 2", 2, history.getSize());
        //        assertEquals("name != Eval1", "Eval1", history.getMemento().getEvaluation().getName());
    }

}
